alpaca-py
python-dotenv
# Pin CrewAI so provider routing cannot change between scheduled runs.
# Gemini is a native provider in CrewAI 1.15, so its optional client must
# be installed explicitly. This fixes the scheduled-run ImportError.
crewai[google-genai]==1.15.5
requests
